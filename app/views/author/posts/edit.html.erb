<% provide(:page_title, "Edit Post #{@post.title}") %>
<% provide(:author, 'active') %>

<h1>Editing Post</h1>

<%= render 'form', post: @post %>

<%= link_to 'Show', author_post_path(@post) %> |
<%= link_to 'Back', author_posts_path %>
